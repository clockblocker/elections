# 2026 snapshot

- **Semantics:** Before changing a crawler, schema, catalog, generated JSON, or the
  interpretation of a field, read [`README.md`](README.md).
- **Declaration corpus:** Before changing document aggregation, paths, duplicate
  handling, or coverage claims, read
  [`docs/adr/0001-index-declaration-documents-in-place.md`](docs/adr/0001-index-declaration-documents-in-place.md).
- **Generated boundary:** Make semantic changes in the Python crawler/generator or
  `regional-declaration-sources.json`, then regenerate the affected output. Preserve
  downloaded declaration documents as exact source artifacts.
- **Completion:** From the repository root, run
  `proper-app/.venv/bin/python -m unittest discover -s proper-data/crawler/tests -v`
  and `git diff --check`. Finish with both checks passing and every changed generated
  file regenerated by its producer.
